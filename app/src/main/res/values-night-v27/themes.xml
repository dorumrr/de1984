<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme for De1984 - Dark Mode (API 27+) -->
    <style name="Theme.De1984" parent="android:Theme.Material.NoActionBar">
        <!-- Status bar -->
        <item name="android:statusBarColor">@color/lineage_teal</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Window background for splash/loading screen with logo - dark background -->
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <!-- AppCompat theme for Views-based activities - Dark Mode (API 27+) -->
    <style name="Theme.De1984.Views" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Status bar -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Enable edge-to-edge -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

        <!-- Window background -->
        <item name="android:windowBackground">@color/background_dark</item>

        <!-- Material 3 color scheme - De1984 teal theme (used when Dynamic Colors are disabled) -->
        <item name="colorPrimary">@color/lineage_teal</item>
        <item name="colorOnPrimary">@color/text_white</item>
        <item name="colorPrimaryContainer">@color/lineage_teal</item>
        <item name="colorOnPrimaryContainer">@color/text_white</item>
        <item name="colorSurface">@color/surface_dark</item>
        <item name="colorOnSurface">@color/text_white</item>
    </style>
</resources>

