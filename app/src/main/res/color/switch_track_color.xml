<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Checked (ON) state - Theme-based track color (outline) -->
    <item android:color="?attr/colorOutline" android:state_checked="true"/>
    <!-- Unchecked (OFF) state - Theme-based track color (outline) -->
    <item android:color="?attr/colorOutline"/>
</selector>

